services:
  cwa-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    restart: always
    environment:
      - DATABASE_URL=file:./dev.db
      - NODE_ENV=production